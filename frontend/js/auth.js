async function login(e){e.preventDefault();const err=document.getElementById('error');err.textContent='';try{const data=await apiRequest('/api/auth/login',{method:'POST',body:JSON.stringify({email:email.value.trim(),password:password.value})});localStorage.setItem('access_token',data.access_token);location.href='dashboard.html'}catch(x){err.textContent=x.message}}
async function register(e){e.preventDefault();const err=document.getElementById('error');err.textContent='';if(password.value.length<6){err.textContent='Mật khẩu nên có ít nhất 6 ký tự.';return}try{await apiRequest('/api/auth/register',{method:'POST',body:JSON.stringify({full_name:fullName.value.trim(),email:email.value.trim(),password:password.value})});location.href='login.html'}catch(x){err.textContent=x.message}}
